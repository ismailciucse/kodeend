import React from "react";

const Title = ({ title }) => {
  return (
    <>
      <div className="main-title text-center">
        <h2>{title}</h2>
      </div>
    </>
  );
};

export default Title;
